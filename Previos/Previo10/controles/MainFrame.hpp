#include <wx/wx.h>

class MainFrame : public wxFrame {
public:
    MainFrame(const wxString& title);
};

// De este archivo se puede observar que se crea la clase MainFrame que hereda de wxFrame, y se declara el constructor de la clase.