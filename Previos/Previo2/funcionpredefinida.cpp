#include <iostream>

// Declaración de la función
void greet() {
    // El cuerpo de la función imprime un mensaje en consola
    std::cout << "Hello World";
}

int main() {
    // Llamada a la función greet desde la función main
    greet();

    return 0;
}
