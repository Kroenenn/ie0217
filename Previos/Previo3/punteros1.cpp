#include <iostream>
using namespace std;

// Definición de la estructura 'temp'
struct temp {
    int i;   // Entero
    float f; // Flotante
};

int main() {
    temp *ptr; // Puntero a 'temp'
    // Aquí podrías hacer algo con 'ptr', como asignarle una dirección de memoria
    return 0;
}
